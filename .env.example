# Verithrax Mark XIII Environment Variables
# Copy this file to .env and fill in your actual values

# OpenAI API Key (get from platform.openai.com/api-keys)
OPENAI_API_KEY=sk-proj-your-key-here

# Replicate API Token (get from replicate.com/account/api-tokens)
REPLICATE_API_TOKEN=r8_your-token-here

# Note: WordPress credentials (WP_BASE_URL, WP_USER, WP_APP_PASSWORD) 
# are now sent dynamically from each WordPress installation via the webhook.
# End users configure these in the WordPress plugin settings page.
